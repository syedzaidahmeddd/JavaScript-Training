const num = [1,2,5,4];
const num2 = (1,2,3); //not array

//isArray();
//Array.isArray(num) ==> True or False
//checks whether given array is array or not

let output = Array.isArray(num2);
console.log(output);