const num = [0,1,2,3,4,5];

//push()

num.pop(6)
console.log(num);

//unshift  = adds at the 0th index or at 1st
//shift = removes from 1st index 
//push = adds at last index or last element
//pop = removes from last element